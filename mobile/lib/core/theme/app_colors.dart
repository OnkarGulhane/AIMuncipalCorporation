import 'package:flutter/material.dart';

/// Stitch Civic Precision Design System Tokens
class AppColors {
  // Brand & Civic Colors (Stitch Civic Precision)
  static const Color primary = Color(0xFF3525CD); // Civic Indigo
  static const Color primaryContainer = Color(0xFF4F46E5);
  static const Color primaryLight = Color(0xFFEEF2FF);
  static const Color primaryDark = Color(0xFF2E1FB8);

  static const Color secondary = Color(0xFF0284C7); // Civic Sky
  static const Color secondaryContainer = Color(0xFFE0F2FE);

  static const Color tertiary = Color(0xFF7E22CE); // Civic Lavender
  static const Color tertiaryContainer = Color(0xFFF3E8FF);

  // Backgrounds & Surfaces (Anti-Glare Light Canvas)
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF1F5F9);
  static const Color surfaceDark = Color(0xFF0F172A);

  // Status & Priority Colors (Pastel sRGB)
  static const Color statusSuccess = Color(0xFF15803D); // Sage Mint
  static const Color statusSuccessSubtle = Color(0xFFDCFCE7);

  static const Color statusWarning = Color(0xFFB45309); // Pastel Amber
  static const Color statusWarningSubtle = Color(0xFFFEF3C7);

  static const Color statusError = Color(0xFFDC2626); // Coral Rose
  static const Color statusErrorSubtle = Color(0xFFFEE2E2);

  static const Color statusInfo = Color(0xFF0284C7); // Sky Info
  static const Color statusInfoSubtle = Color(0xFFE0F2FE);

  // Neutrals & Text Hierarchy (Inter Optimized)
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderDashed = Color(0xFFCBD5E1);
  static const Color divider = Color(0xFFF1F5F9);
}

