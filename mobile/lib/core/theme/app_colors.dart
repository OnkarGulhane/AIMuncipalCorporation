import 'package:flutter/material.dart';

class AppColors {
  // Brand & Civic Colors
  static const Color primary = Color(0xFF1E3A8A); // Deep Civic Navy
  static const Color primaryLight = Color(0xFF3B82F6);
  static const Color primaryDark = Color(0xFF172554);

  static const Color secondary = Color(0xFF0D9488); // Municipal Teal
  static const Color accent = Color(0xFF6366F1); // Indigo AI Accent

  // Backgrounds & Surfaces
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF0F172A);

  // Status & Priority Colors
  static const Color statusSuccess = Color(0xFF10B981); // Emerald Green
  static const Color statusWarning = Color(0xFFF59E0B); // Amber
  static const Color statusError = Color(0xFFEF4444); // Crimson Red
  static const Color statusInfo = Color(0xFF3B82F6); // Blue

  // Neutrals & Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color border = Color(0xFFE2E8F0);
  static const Color divider = Color(0xFFF1F5F9);
}
